import 'package:flutter/material.dart';

// Minimal, calm, and professional color palette (No gradients)

// Primary Colors
const Color primaryColor = Color(0xFF4CAF50);  // Soft Green
const Color secondaryColor = Color(0xFF2196F3); // Muted Blue
const Color accentColor = Color(0xFFFF9800);    // Soft Orange

// Background Colors
const Color backgroundColor = Color(0xFFF5F5F5); // Light Grey (background)
const Color surfaceColor = Color(0xFFFFFFFF);    // Pure white for cards and surfaces

// Text Colors
const Color primaryTextColor = Color(0xFF212121);   // Dark Charcoal
const Color secondaryTextColor = Color(0xFF757575); // Medium Grey

// Card Background (no glassmorphism)
const Color cardBackgroundColor = Color(0xFFFFFFFF); // Solid white for clarity

// Shadow for subtle depth
const Color shadowColor = Color(0x1F000000); // Very soft black shadow

// Status Colors (optional)
const Color successColor = Color(0xFF388E3C);   // Calm Green for success
const Color warningColor = Color(0xFFFBC02D);   // Soft Amber for warnings
const Color errorColor = Color(0xFFD32F2F);     // Muted Red for errors
